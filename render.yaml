services:
  - type: web
    name: casemind-backend
    env: docker
    dockerfilePath: casemind-backend/Dockerfile
    plan: free
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    buildCommand: ""
    envVars: []
